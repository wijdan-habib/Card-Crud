import React, { useState } from 'react'
import axios from 'axios'
export default function CardComp() {
    const [card, setCard] = useState();
    try {
        const displayCard = axios
    } catch (error) {
        
    }
  return (
    <div>

    </div>
  )
}
